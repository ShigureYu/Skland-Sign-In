name: Keepalive Workflow

on:
  schedule:
    # 每月 1 号 midnight 自动运行一次
    - cron: '0 0 1 * *'
  workflow_dispatch: # 支持手动触发

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: gautamkrishnar/keepalive-workflow@v2 # 专门用来防止仓库休眠的社区标准 Action
